pymupdf==1.28.2
pandas==3.0.5
fastapi==0.141.1
uvicorn[standard]==0.53.0
pytest==9.1.1

# Only needed for the OCR fallback path (scanned PDFs) -- see
# backend/ocr_convert.py and CLAUDE.md.
docling==2.128.0
